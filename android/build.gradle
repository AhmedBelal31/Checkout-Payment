    allprojects {
        repositories {
            google()
            mavenCentral()
        }
    }

    rootProject.buildDir = '../build'
    subprojects {
        project.buildDir = "${rootProject.buildDir}/${project.name}"
    }
    subprojects {
        project.evaluationDependsOn(':app')
    }

    tasks.register("clean", Delete) {
        delete rootProject.buildDir
    }

//allprojects {
//    repositories {
//        google()
//        mavenCentral()
//    }
//}
//
//rootProject.buildDir = '../build'
//
//subprojects {
//    project.buildDir = "${rootProject.buildDir}/${project.name}"
//    project.evaluationDependsOn(':app')
//
//    // Add dependency overrides for the conflicting dependency
//    configurations.all {
//        resolutionStrategy {
//            // Exclude Kotlin dependencies causing version mismatch
//            failOnVersionConflict()
//            force "org.jetbrains.kotlin:kotlin-stdlib:1.8.10" // Specify the version you want to use
//        }
//    }
//}
//
//tasks.register("clean", Delete) {
//    delete rootProject.buildDir
//}
